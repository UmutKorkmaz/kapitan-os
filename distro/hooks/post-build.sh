#!/usr/bin/env bash
# KAPiTaN OS — post-build hook (placeholder)
#
# Invoked after live-build produces the chroot/ISO artifact.
# Future steps: locale-gen verification, /etc/kapitan/VERSION stamp, artifact checksums.

set -euo pipefail

echo "distro/hooks/post-build.sh: placeholder — no finalize steps yet (F0 skeleton)"